{
  "_comment": "Single source of truth for all front/ repo dependencies. Read by build.sh deps + GHA ship.yml.",
  "system": {
    "required": {
      "node": { "nix": "nodejs_22", "apt": "nodejs" },
      "npm":  { "nix": "nodejs_22", "apt": "npm" },
      "git":  { "nix": "git",       "apt": "git" }
    },
    "optional": {
      "rsync":  { "nix": "rsync",  "apt": "rsync" },
      "rclone": { "nix": "rclone", "apt": "rclone" }
    }
  },
  "node": {
    "_comment": "Node packages resolved dynamically by scanning all project package.json files."
  },
  "gha": {
    "nix_channel": "nixos-24.11"
  }
}
