name: GitHub Pages Deployment (CI)

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Needed to compare changes

      - name: 📂 Get Changed Files
        id: changed-files
        uses: tj-actions/changed-files@v41

      # ============================================
      # SASS COMPILATION (Conditional)
      # ============================================
      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            1.ops/package-lock.json

      - name: 🎨 Build CSS from Sass (Root)
        if: steps.changed-files.outputs.any_changed && contains(steps.changed-files.outputs.all_changed_files, '3.sass/')
        run: |
          echo "📁 Building root Sass..."
          cd 1.ops
          npm install
          npm run sass:build
          cd ..
          echo "✅ Root style.css created"

      - name: 🎨 Build CSS from Sass (cv_web)
        if: steps.changed-files.outputs.any_changed && contains(steps.changed-files.outputs.all_changed_files, 'cv_web/3.sass/')
        run: |
          echo "📁 Building cv_web Sass..."
          cd 1.ops
          npm install
          # This should be adapted if cv_web has a different build script
          # Assuming it uses the same for now
          npm run sass:build 
          cd ..
          echo "✅ cv_web/style.css created"

      # ============================================
      # TYPESCRIPT COMPILATION (Conditional)
      # ============================================
      - name: 📜 Build JS from TypeScript
        if: steps.changed-files.outputs.any_changed && contains(steps.changed-files.outputs.all_changed_files, '4.ts/')
        run: |
          echo "📜 Building TypeScript..."
          cd 1.ops
          npm install
          npm run ts:build
          cd ..
          echo "✅ script.js created"

      # ============================================
      # MYFEED BUILD (Vue 3 + Vite - Conditional)
      # ============================================
      - name: 🔨 Build MyFeed (Vue 3 + Vite)
        if: steps.changed-files.outputs.any_changed && contains(steps.changed-files.outputs.all_changed_files, 'myfeed/')
        run: |
          echo "🔨 Building MyFeed Vue application..."
          cd myfeed
          npm install
          npm run build
          echo "✅ MyFeed Vue build complete"

      - name: 🔨 Build MyProfile (SvelteKit)
        if: steps.changed-files.outputs.any_changed && contains(steps.changed-files.outputs.all_changed_files, 'myprofile/')
        run: |
          echo "🔨 Building MyProfile SvelteKit project..."
          cd myprofile/1.3.svelte
          npm install
          npm run build
          cd ../..
          echo "✅ MyProfile build complete"

      - name: 🌐 Setup Pages Environment
        uses: actions/configure-pages@v5

      - name: 📂 Prepare Site for Deployment
        run: |
          echo "📋 Creating _site directory and copying all files..."
          mkdir -p _site
          # Copy all relevant files and directories (excluding myfeed for now)
          cp -r index.html style.css script.js linktree/ cv_web/ cv_pdf/ assets/ others/ _site/ 2>/dev/null || true

          # Copy MyFeed Vue build (if dist exists)
          if [ -d "myfeed/dist" ]; then
            echo "📦 Copying MyFeed Vue build..."
            mkdir -p _site/myfeed
            cp -r myfeed/dist/* _site/myfeed/
            echo "✅ MyFeed Vue build copied to _site/myfeed/"
          else
            echo "⏭️  Skipping MyFeed copy (no dist directory found)"
          fi

          # Copy MyProfile SvelteKit build (if build exists)
          if [ -d "myprofile/1.3.svelte/build" ]; then
            echo "📦 Copying MyProfile SvelteKit build..."
            mkdir -p _site/myprofile
            cp -r myprofile/1.3.svelte/build/* _site/myprofile/
            echo "✅ MyProfile build copied to _site/myprofile/"
          else
            echo "⏭️  Skipping MyProfile copy (no build directory found)"
          fi

          echo "✅ All files prepared for deployment"
          echo "📊 Site structure:"
          ls -lah _site/
          echo ""
          echo "📊 MyFeed contents:"
          ls -lah _site/myfeed/ 2>/dev/null || echo "No myfeed directory"

      - name: ⬆️ Upload Site Artifact
        uses: actions/upload-pages-artifact@v3

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4